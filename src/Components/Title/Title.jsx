import React from 'react';

const Title = ({ Header }) => {
  return (
    <div>
      <h2 className='text-center text-3xl font-bold uppercase '>{Header}</h2>
    </div>
  );
};

export default Title;
